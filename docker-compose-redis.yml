version: "3.9"

services:
  redis:
    image: redis:7
    container_name: my_redis
    ports:
      - "6379:6379"
    networks:
      - myapp-network
    restart: unless-stopped

networks:
  myapp-network:
    external: true